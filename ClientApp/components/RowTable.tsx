import * as React from 'react';
import { Phone } from './PhoneTable';

interface RowProps {
    phone: Phone
    edit: boolean
}
export const RowTable: React.SFC<RowProps> = (props) => {

    return <h1>hello</h1>
}